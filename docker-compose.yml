version: '3.8'

services:
  mikroguard:
    image: omtacmen/mikroguard-gui:latest
    container_name: mikroguard-gui
    restart: always
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data